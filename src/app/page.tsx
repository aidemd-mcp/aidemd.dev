/**
 * Marketing landing page — full section composition built in steps 5–9.
 * This shell exists so `npm run build` produces out/index.html for the step 1 acceptance gate.
 */
export default function HomePage() {
  return (
    <main>
      <p>aidemd.dev — coming soon</p>
    </main>
  );
}

# For local dev: copy this file to .env.local to load GTM in dev mode.
# Production builds get NEXT_PUBLIC_GTM_ID from the GTM_ID repo secret (see .github/workflows/deploy.yml).
NEXT_PUBLIC_GTM_ID=GTM-M24M4PPV

# Site origin (used for canonical URLs and metadataBase)
NEXT_PUBLIC_SITE_URL=https://aidemd.dev
